sudo: required
language: bash
before_install: |
  curl -Ls https://convox.com/install/linux.zip > /tmp/convox.zip &&\
  sudo unzip /tmp/convox.zip -d /usr/local/bin
after_success:
  - convox deploy --app convoxapp --rack nongod/convoxrack
notifications:
  slack:
    rooms:
      - 104corp:IYsPMQMlzQSNSvBU9a1JM21h#travis_integration
    on_success: always
    on_failure: always
